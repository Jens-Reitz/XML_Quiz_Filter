<Window x:Class="QuizFilterApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
        Title="Quiz Filter App" Height="450" Width="800">
    <Window.Resources>
        <CollectionViewSource x:Key="QuestionsView"
                        Source="{Binding Questions}">
            <CollectionViewSource.SortDescriptions>
                <!-- Kategorien zuerst (true > false) -->
                <scm:SortDescription PropertyName="IsCategory" Direction="Descending" />
                <!-- Danach optional alphabetisch nach Text -->
                <scm:SortDescription PropertyName="Text"       Direction="Ascending" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Import Button -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Button Content="XML Importieren" Grid.Row="0" Margin="0,0,0,10" Width="760" Height="30" Click="ImportXml_Click" />
        </StackPanel>

        <!-- Filter Section -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <TextBox x:Name="FilterTextBox" Width="300" Margin="0,0,10,0" Text="" />
            <Button Content="Filter anwenden" Width="220" Click="ApplyFilter_Click" />
            <Button Content="Liste leeren" Height="30" Width="220" Margin="10,0,0,0" Click="ClearList_Click" />
        </StackPanel>

        <!-- Questions List -->
        <ListBox x:Name="QuestionsListBox"
         Grid.Row="2"
         SelectionMode="Extended"
         MouseDoubleClick="QuestionsListBox_MouseDoubleClick"
         ItemsSource="{Binding Source={StaticResource QuestionsView}}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <!-- Checkbox -->
                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                          Margin="5" />

                        <!-- TextBlock für Kategorien -->
                        <TextBlock Text="{Binding Text}"
                           VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Normal" />
                                    <Setter Property="Foreground" Value="Black" />
                                    <Style.Triggers>
                                        <!-- Wenn IsCategory == true, dann fett und blau -->
                                        <DataTrigger Binding="{Binding IsCategory}" Value="True">
                                            <Setter Property="FontWeight" Value="Bold" />
                                            <Setter Property="Foreground" Value="DarkBlue" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <!-- Export Button -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,0,0,10">
            <Button Content="Exportieren"  Margin="0,10,0,0" Height="30" Width="760" Click="ExportQuestions_Click" />
        </StackPanel>
    </Grid>
</Window>